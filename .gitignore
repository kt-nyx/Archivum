.cursor/
.agents/
.claude/
.codex/
.github/
Reference/

# Local-only companion repository. Keep both names ignored so the private
# workflow can never be staged during or after the Archivum rename.
/archivum-workflow/
/lore-workflow/

# Cache folders
__pycache__/
.cache/
cache/
.mypy_cache/
.pytest_cache/
.ruff_cache/
*.py[cod]
.venv/
.vscode

# Local secrets and overrides (use `.env.example` as the template)
.env
.env.local

# Pipeline run outputs (reproducible locally; not source)
artifacts/
pipeline/discovery/.world_registry_category_cache.json
pipeline/data/.section_label_harvest_cache.json
pipeline/data/.section_label_harvest_cache_v2.json
