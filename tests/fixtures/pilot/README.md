# Regression Input Fixtures

`source_manifest.json` is retained as an ingest-input fixture. It is not a promotion
baseline, output oracle, or runtime dependency. Questline, instance, and prose output is
validated through zone-neutral structure, evidence, provenance, and retail-eligibility tests.

No fixture in this directory may prescribe emitted card IDs, counts, titles, anchors, or prose.
